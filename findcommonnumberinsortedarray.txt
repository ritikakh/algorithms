// find common number in 2 sorted arrays

var arr1 = [2,3,4,6,7];
var arr2 = [1,2,8,9];

var newArr = arr1.concat(arr2).sort(function(a,b) {
	return a-b;
});

for(var i = 0; i < newArr.length; i++) {
	if(newArr[i] === newArr[i+1]) {
  	console.log(newArr[i]);
    break;
  }
}