var arr = [3,4,5,2,6];
var differ = {};
var sum = 9;
var value;

function checkPair(arr, sum) {
  for(var i = 0; i < arr.length; i++) {
    value = sum - arr[i];
    if(differ.value) {
      return true;
    }
    else {
      return differ[arr[i]] = true;
    }
  }
}

console.log(checkPair(arr, sum));